apiVersion: v1
kind: Service
metadata:
  name: expense-service
spec:
  type: NodePort
  selector:
    app: expense-tracker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007